#include "main.h"
#include <stdio.h>

/** _isdigit - check if the parameter isdigit or characteer
*
* c = parameter
* return : 1 if is digit or 0 otherwise
*/

int _isdigit(int c)
{
	if (c >= 0 && c <=9)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}

